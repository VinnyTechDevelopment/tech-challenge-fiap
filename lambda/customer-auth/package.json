{
  "name": "customer-auth-lambda",
  "version": "1.0.0",
  "private": true,
  "description": "Function Serverless que valida CPF, consulta existencia/status do cliente e emite JWT para as APIs protegidas do POS Tech.",
  "main": "src/handler.js",
  "scripts": {
    "test": "jest"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.11.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
